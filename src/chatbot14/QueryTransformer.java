public class QueryTransformer
{
    /*
     * Param is placeholder for when we actually need to transform query
     * This is where we will take a String array as input, transform it,
     * and return it as a String array.
     */
    public String[] transform(String[] x)
    {
        //This is just a temporary array so it compiles
        String[] transformedQuery = {"G", "I"};
        
        return transformedQuery;
    }
}
